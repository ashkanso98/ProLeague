@* @model IEnumerable<ProLeague.Domain.Entities.News>

@{
    ViewData["Title"] = "مدیریت اخبار";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <h1 class="h3">@ViewData["Title"]</h1>
    <a asp-action="Create" class="btn btn-primary">
        <i class="fas fa-plus me-2"></i> ایجاد خبر جدید
    </a>
</div>

@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success alert-dismissible fade show" role="alert">
        @TempData["SuccessMessage"]
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
}

<div class="card shadow-sm">
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-hover align-middle">
                <thead class="table-light">
                    <tr>
                        <th>تصویر اصلی</th>
                        <th>عنوان خبر</th>
                        <th>تاریخ انتشار</th>
                        <th class="text-center">خبر ویژه</th>
                        <th class="text-center">عملیات</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                <img src="@(item.MainImagePath ?? "/images/default-news.png")" alt="@item.Title"
                                     style="width: 100px; height: 60px; object-fit: cover; border-radius: .25rem;" />
                            </td>
                            <td>@item.Title</td>
                            <td>@item.PublishedDate.ToString("yyyy/MM/dd HH:mm")</td>
                            <td class="text-center">
                                @if (item.IsFeatured)
                                {
                                    <span class="text-warning fs-5"><i class="fas fa-star"></i></span>
                                }
                            </td>
                            <td class="text-center">
                                <div class="btn-group" role="group">
                                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-outline-secondary" title="ویرایش"><i class="fas fa-edit"></i></a>
                                    <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm btn-outline-info" title="جزئیات"><i class="fas fa-info-circle"></i></a>
                                    <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-outline-danger" title="حذف"><i class="fas fa-trash-alt"></i></a>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div> *@

                                    

@model IEnumerable<ProLeague.Domain.Entities.News>

@{
    ViewData["Title"] = "مدیریت اخبار";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <h1 class="h3">@ViewData["Title"]</h1>
    <a asp-action="Create" class="btn btn-primary">
        <i class="fas fa-plus me-2"></i> ایجاد خبر جدید
    </a>
</div>

@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success alert-dismissible fade show" role="alert">
        @TempData["SuccessMessage"]
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
}

<div class="card shadow-sm">
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-hover align-middle">
                <thead class="table-light">
                    <tr>
                        <th>تصویر اصلی</th>
                        <th>عنوان خبر</th>
                        <th>تاریخ انتشار</th>
                        <th class="text-center">خبر ویژه</th>
                        <th class="text-center">عملیات</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                <img src="@(item.MainImagePath ?? "/images/default-news.png")" alt="@item.Title"
                                     style="width: 100px; height: 60px; object-fit: cover; border-radius: .25rem;" />
                            </td>
                            <td>@item.Title</td>
                            <td>@item.PublishedDate.ToString("yyyy/MM/dd HH:mm")</td>
                            <td class="text-center">
                                @if (item.IsFeatured)
                                {
                                    <span class="text-warning fs-5" title="خبر ویژه"><i class="fas fa-star"></i></span>
                                }
                            </td>
                            <td class="text-center">
                                <div class="btn-group" role="group">
                                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-outline-secondary" title="ویرایش"><i class="fas fa-edit"></i></a>
                                    <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm btn-outline-info" title="جزئیات"><i class="fas fa-info-circle"></i></a>
                                    <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-outline-danger" title="حذف"><i class="fas fa-trash-alt"></i></a>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>